import { SearchMode } from '../types';

export default [{
  name: 'Line contains',
  id: SearchMode.LINE_CONTAINS,
}, {
  name: 'Line contains regex match',
  id: SearchMode.LINE_CONTAINS_REGEX_MATCH,
}];
